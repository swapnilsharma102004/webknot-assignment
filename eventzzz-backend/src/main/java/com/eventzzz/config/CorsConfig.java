package com.eventzzz.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class CorsConfig {
    // CORS configuration is now handled in SecurityConfig to avoid bean conflicts
}
